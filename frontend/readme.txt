This is the front end app served out at 127.0.0.1:8080.

## Installing

Install the latest version of node(should come with npm).

* `npm install` within the project directory to install all dependencies

## Running the app

* `npm start` to build the app and start the server

